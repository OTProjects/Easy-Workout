import React from 'react';
import ReactDOM from 'react-dom/client';
import SimpleWorkoutPlanner from './SimpleWorkoutPlanner';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SimpleWorkoutPlanner />
  </React.StrictMode>
);