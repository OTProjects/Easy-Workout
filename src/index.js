import React from 'react';
import ReactDOM from 'react-dom/client';
import ModernWorkoutPlanner from './ModernWorkoutPlanner';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ModernWorkoutPlanner />
  </React.StrictMode>
);