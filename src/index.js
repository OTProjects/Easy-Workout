import React from 'react';
import ReactDOM from 'react-dom/client';
import WorkoutApp from './workout-planner';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WorkoutApp />
  </React.StrictMode>
);